import actions from './actions';

const deselectRow = actions.deselectRow;
const fetchData = actions.fetchData;
const selectRow = actions.selectRow;

export default { deselectRow, fetchData, selectRow };
